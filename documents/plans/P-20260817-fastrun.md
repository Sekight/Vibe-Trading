# 计划：回测 fastrun——可跳过 regime / MAE-MFE 分析

> 编号：P-20260817-fastrun
> 状态：讨论中
> 日期：2026-08-17
> 关联迭代：待填（收尾时填 V 号）
> 关联：commit / run（收尾时补）

## 项目调研

- 3 年 5m 回测缓存命中总耗时 766s，其中回测后 digest（分析数据生成）约 750s 占 98%（引擎执行仅 ~14s）；1 个月 5m 全程 16s（2026-08-17 实测，见全局 E010 / Mistake_Journal M027）。
- digest 慢的两个组件（cProfile 定位）：`compute_edge_density`（agent/backtest/regime.py，每个 bar 做一次 60-bar 窗口滚动 pandas corr，O(bar×window)）与 `add_mae_mfe`（agent/backtest/analysis/digest.py，每笔交易遍历该标的全量 bar 筛窗口，O(trades×bars)）。
- digest 消费方：WebUI 分析图/分析标签从 `analysis.digest.json` 取数；相关性 regime 另有独立端点 `/correlation/regime`（system_routes.py，与本需求无关，不动）。前端组件 RegimeTimeline.tsx / 分析图图表读取 digest 字段（2026-08-17 初步 grep，缺失容错点为执行清单第 1 步）。

## 需求目标

- 做什么：给直接跑回测的入口 `python -m backtest.runner <run_dir>` 增加三个指令（CLI flag），控制回测后 digest 的计算内容：
  - `--without-regime`：不计算相关性 regime 分析（edge density / FUSED 状态机），digest 省略对应字段与 markdown 段落；
  - `--without-mae-mfe`：不计算 MAE/MFE（逐笔 bar 级极值），digest 省略对应字段；
  - `--fastrun`：以上两个都不输出（等价组合开关），且设计为**可扩展**——以"跳表/集合"形式实现，以后发现 digest 或其他阶段有耗时步骤，直接追加进 fastrun 跳表即可，一切为了尽快跑出回测与核心指标（metrics.csv / trades.csv / run_card）。
- 范围 / 边界：
  - 只改回测 runner 与 digest 构建（agent/backtest/runner.py、agent/backtest/analysis/digest.py）及其消费方容错；**不动引擎执行、不动 metrics 核心指标、不动 trades.csv/equity.csv 产物**。
  - 独立端点 `/correlation/regime` 不纳入；WebUI 交互页面的指标卡/交易表/图表主图不因跳过而变化（只有依赖被跳字段的分析图/分析报告部分降级）。
  - 默认行为不变：不加 flag 时 digest 与现状完全一致。
  - config.json 不新增字段（纯 CLI flag）；`--fastrun` 与两个独立 flag 可组合使用（如 `--fastrun` 已含全部，再显式传独立 flag 不报错）。
- 验收标准：`--fastrun` 跑 3 年 5m 总耗时从 766s 降到 1 分钟内（预期主要剩 fetch/generate/run_backtest ~18s + 精简 digest）；`metrics.csv` / `trades.csv` / `run_card.json` 与完整版完全一致；digest 缺失 regime/mae_mfe 字段时 WebUI 分析图与分析报告正常降级不报错。

## 实现方案

（涉及文件/模块、关键设计；讨论中随时补充）

1. **digest.py**：`build_digest(run_dir, *, include_regime=True, include_mae_mfe=True)`——对应跳过 `_regime_from_run(...)` 与 `add_mae_mfe(...)` 的调用；被跳过的数据在 digest 结构与 markdown 渲染（`_markdown_*` 段）中一并省略，不输出空字段。
2. **runner.py**：`__main__` 增加 `--without-regime` / `--without-mae-mfe` / `--fastrun` 三个 argparse flag，构建 skip 集合（如 `skips: set[str]`，`--fastrun` 预置 `{"regime", "mae_mfe"}`），传给 digest 构建调用点；`--fastrun` 的跳表用模块级常量（如 `FASTRUN_SKIPS = {"regime", "mae_mfe"}`），后续新耗时步骤追加此集合即可，无需改调用逻辑。
3. **消费方容错**（执行清单第 1 步调研确定范围）：后端 analysis 路由 / 前端分析图组件对缺失的 regime、mae_mfe 字段按"缺失即不渲染/标 N/A"处理，与旧 run 缺字段的既有容忍逻辑对齐。
4. 文档：HowToUse 8.38（回测提速段）补充 fastrun 三个 flag 的说明与实测耗时对比。

## 执行清单

1. 调研 digest 消费方（后端 analysis 路由 + 前端 RunDetail 分析图/分析标签组件）对 regime、mae_mfe 字段的读取点，确定缺失时的容错位置与现状（旧 run 缺字段是否已容忍）。
2. digest.py：`build_digest` 加 include 参数，跳过 regime / mae_mfe 计算并省略对应 digest 字段与 markdown 段落。
3. runner.py：`__main__` 加三个 flag + `FASTRUN_SKIPS` 跳表，透传到 digest 构建。
4. 按调研结果补消费方容错（缺失字段不渲染 / 标 N/A）。
5. 验证：
   - 功能：`--fastrun` 跑 1 个月 5m run（新副本），trades/metrics/equity 与完整版一致；digest 无 regime/mae_mfe 字段；WebUI 分析图/分析标签正常。
   - 性能：3 年 5m `--fastrun` 计时（预期 <60s vs 766s）；单独 `--without-regime` 与 `--without-mae-mfe` 各计时，确认各自贡献占比。
   - 回归：后端相关 pytest（analysis digest/charts/api + runner）+ 前端 vitest 全量 + `npm run build`。
6. 更新 HowToUse 8.38。

## 开工前核对

（状态从"讨论中"切到"已确认"前由 Codex 逐项核对；核对结果按清单逐项展示"通过 / 未通过 + 发现项"）

- 需求目标 / 范围与讨论记录一致
- 范围/边界无被后续讨论反转但仍保留的旧约束
- 执行清单覆盖需求目标与验收标准
- 验收标准可验证
- 元信息已填（关联允许为待填）

## 验证

- 性能：`cd agent && VIBE_TRADING_DATA_CACHE=1 ../.venv/Scripts/python.exe -m backtest.runner "<vibe_home>\runs\ta_turtle_5m_v4_2021_2023" --fastrun` 计时，对比完整版 766s。
- 一致性：完整版 vs `--fastrun` 的 `artifacts/metrics.csv` 与 `trades.csv` 逐字段一致（diff 为空）。
- 功能：digest 缺失 regime/mae_mfe 时 WebUI 打开 run 的分析图/分析标签无报错，相关图表降级显示。
- 回归：`pytest`（analysis digest/charts/api、runner 相关）+ `cd frontend && npm test` + `npm run build`。

## 讨论记录

（append-only：谁提出、选项、结论；范围/边界反转时标注"范围变更：原=... → 现=..."）

- 2026-08-17 用户提出：对跑回测加 3 个指令（without regime / without MAE-MFE / fastrun），fastrun 定位为"以后发现哪步耗时就把那步也纳入 fastrun"，一切为尽快跑出 metric。

## 风险 / 注意

- digest 缺失字段的兼容：旧 run 的完整 digest 不受影响；fastrun 产生的 digest 若被旧前端读取需容错（执行清单第 1 步确认现状）。
- `--fastrun` 只是跳过分析，不改变回测与指标口径；MAE/MFE 图、regime 时间线在 fastrun run 中不可见属预期。
- 单标的 run 本就不产生有意义的 regime（需 ≥2 标的），`--without-regime` 对这类 run 无额外收益但也不报错。
