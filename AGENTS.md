# Vibe-Trading 项目 AGENTS（项目全局工作台）

> 本项目专属规则。若环境中存在全局 AGENTS / 全局工作台，按其跨项目规则执行；未提供时，本文件为项目唯一规则源。
> 本文件是项目唯一规则源；其他文档只放模板、索引、状态表。

> 路径约定：仓库内文档一律用相对路径（如 documents/plans/）；机器相关路径用 <repo_root> / <vibe_home> 占位，不写绝对路径。

## 一、核心规则

1. **计划闸门**：大/中改动先写计划，讨论、完成开工前核对并按清单逐项展示“通过 / 未通过 + 发现项”、你确认后才写码；你确认后，agent 先更新计划状态为“已确认”（含计划 README 当前计划表），再开始写码；小改动不建计划。
2. **收尾留痕**：完成后更新计划状态与关联编号（含计划 README 索引）；该记录的迭代（大/中、含决策/坑/行为变化、影响用户可见）写 ITERATION_LOG（5 字段模板），纯机械/纯文档小改不写、靠 git commit 留痕；用户使用方式变化同步 HowToUse；任务中发现坑的，收尾先入 Mistake_Journal（新坑建条目、存量坑更新状态），解法需要时再进 HowToUse。
3. **编号规则**：写 ITERATION_LOG 前重读文件，编号 = 当前最大 + 1，写后复读校验，撞号则后写者改为当前最大 + 1，已删除编号不补号；计划目录（README / 计划文档）写入前也重读并合并，不覆盖他人内容；计划编号 P-... 唯一；计划文档“关联迭代”只写“待填”，不预填具体 V 号。
4. **文档分流**：用户使用 Vibe-Trading 的问题只入 HowToUse；vibe-trading 专属坑（认知与状态）入 Mistake_Journal；跨项目通用、有复用价值的坑才写全局日志（若环境提供全局复利与踩坑日志）。
5. **真相与边界**：文档是索引，代码是真相，冲突以代码为准；关键改动跑最窄验证；恢复进度以 git status / git diff 为准，不维护额外进度状态；不覆盖用户未提交改动；不提交 .env / run artifacts / 本地数据 / 私人笔记。

## 二、文档地图

| 文档 | 角色 | 维护策略 |
|---|---|---|
| README（5 语言） | 用户入口、项目结构、API 表 | 用户要求时才更新 |
| CHANGELOG / CONTRIBUTING / SECURITY / AGENT_CONTRIBUTOR_GUIDE | 上游文档 | 暂不更新 |
| HowToUse.md | 怎么用、FAQ、坑 | 主动维护 |
| ITERATION_LOG.md | 过去做了什么、为什么、关键结论 | 主动维护 |
| Mistake_Journal.md | 项目专属坑的认知与状态账本（坑的入口） | 主动维护：坑先入账本、状态权威、单向指针 |
| documents/plans/ | 开工前计划（README=状态与索引入口；_template.md=新建计划模板） | 主动维护 |

## 三、查找规则

- 新需求 / 新迭代 / 未完成需求：先看计划目录（README 为入口，新建计划用 _template.md）
- 回顾过去：先查 ITERATION_LOG，按“关联”找计划文档
- 坑的认知与状态 / 复利复盘：查 Mistake_Journal（开工前可扫一眼索引防重复踩坑）
- 使用问题：查 HowToUse
- 冲突裁决：代码 + git history

## 四、计划分级

- 大迭代（新功能、新数据源、引擎/路由、跨模块）：完整计划文档（含开工前核对）
- 中改动（单模块修复、优化、脚本）：精简计划文档（需求目标/实现方案/执行清单 + 验证 + 开工前核对）
- 小改动（文档、调参、单文件小修）：不建计划、不涉及核对
- 新建计划：用 `_template.md`，文件名 `P-YYYYMMDD-短标题`（短标题用 `_`），创建前先重读计划 README 检查编号/同名冲突；创建后同步登记到计划 README 当前计划表
- 拿不准时按大/中处理