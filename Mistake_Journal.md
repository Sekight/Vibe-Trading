# Vibe-Trading 踩坑日志（Mistake_Journal）

> 定位：vibe-trading **项目专属**坑的认知与状态的唯一账本（踩坑教训持续累积、不再重复踩 = 认知复利），也是坑的入口（坑发现时先入本账本）。
> 项目规则源：`AGENTS.md`（仓库根）。本文件与 ITERATION_LOG（历史叙事）、HowToUse（使用解法）、全局复利与踩坑日志（跨项目经验）分工如下。

## 分工判据（同一个坑只在一处有认知）

| 坑的性质 | 落点 |
|---|---|
| 跨项目通用经验（agent 行为、方法论） | 全局复利与踩坑日志（E 编号） |
| 使用障碍与解法（怎么用、报错怎么办） | HowToUse 8.x |
| 迭代叙事（为什么改、改的过程） | ITERATION_LOG（V 编号） |
| vibe-trading 专属坑（认知与状态） | **本文件（M 编号）** |

## 维护规则

1. **入口**：坑发现时先入本账本（新坑建条目、存量坑更新状态）；解法需要时再进 HowToUse。
2. **状态权威**：坑是否修复以本账本状态为准；HowToUse 的日期标注只是使用侧时效提示，不一致时以本账本为准。
3. **单向指针**：本文件条目可指向 HowToUse 8.x / ITERATION_LOG V 号 / 全局 E 号；其他文件不反向逐条指向本文件（HowToUse 第 11 节有一处总入口）。
4. **修复留痕**：修复时在条目内追加一行 `已修复：日期 · 证据（commit / V0XX / 新做法）`，原文保留不删。
5. **结论废弃**：认知被推翻时标注 `结论已废弃，以 M0XX 为准`，原文保留。
6. **编号**：M001 起全局递增，写前重读、写后复读校验；已删除不补号。
7. **定期补充**：每周日自动任务输出的坑/问题清单，经用户确认后入本账本。

## 条目模板

```markdown
- M0XX · <坑名>（发现日期） · 分类
  现象：…
  教训：…
  状态：有效 / 已修复（日期，证据）/ 结论已废弃
  关联：解法见 HowToUse 8.x / 来源 V0XX / 同全局 E0XX（按需，没有不写）
```

## 索引

| 编号 | 日期 | 分类 | 一句话摘要 |
|---|---|---|---|
| M001 | 2026-08-10 | 🟡坑 | 腾讯长区间静默截断 500 根 |
| M002 | 2026-08-07 | 🟡坑 | cmd 下 Rich 输出 ANSI 乱码 |
| M003 | 2026-08-07 | 🟡坑 | 日志乱码"驱动器 E 中的卷没有标签" |
| M004 | 2026-08-07 | 🟡坑 | run_dir outside allowed run roots |
| M005 | 2026-08-07 | 🟡坑 | 东财 suggest 接口返回 JSONP 兜底页 |
| M006 | 2026-08-07 | 🟡坑 | 改 Python 源码后 WebUI 不生效 |
| M007 | 2026-08-07 | 🟡坑 | SSE 流式中断 WinError 10054 |
| M008 | 2026-08-07 | 🔴错误 | 策略任务烧 token + 前端"等待后已停止" |
| M009 | 2026-08-08 | 🟡坑 | data-bridge config.yaml 被存成二进制 |
| M010 | 2026-08-08 | 🟡坑 | source=local 报 missing local: 前缀 |
| M011 | 2026-08-12 | 🟡坑 | identity_mismatch / 沪深300 基准取不到 |
| M012 | 2026-08-08 | 🔴错误 | 显示 SUCCESS 但无报告 |
| M013 | 2026-08-10 | 🟡坑 | loader 缓存不过期/坏数据反复命中 |
| M014 | 2026-08-12 | 🟡坑 | trades.csv 被 WPS/Excel 占用 PermissionError |
| M015 | 2026-08-12 | 🟡坑 | 前/后复权口径混淆 |
| M016 | 2026-08-14 | 🟡坑 | backtest_end 纯日期截掉最后一天 |
| M017 | 2026-08-14 | 🟡坑 | K 线 tooltip OHLC 错位 |
| M018 | 2026-08-15 | 🔴错误 | 交易表硬编码列导致新列不渲染 |
| M019 | 2026-08-15 | 🟡坑 | 策略文件被外部进程置零损坏 |
| M020 | 2026-08-16 | 🔴错误 | 触轨/止盈判定口径与预期相反 |
| M021 | 2026-08-16 | 🟡坑 | 郑商所后缀 .CZCE 误判 A 股引擎、0 成交无警告 |
| M022 | 2026-08-16 | ✅已修复 | 直跑 runner 不加载 vibe_home/.env，缓存开关失效 |
| M023 | 2026-08-16 | 🟡坑 | 保本止损同 bar 回溯假象（bar 级顺序假设） |
| M024 | 2026-08-16 | 🟡坑 | 主连切换跳空污染切换后约 20 根指标 |
| M025 | 2026-08-16 | 🟡坑 | trades.csv 归因陷阱：reason 失真 + qty 随权益漂移 |
| M026 | 2026-08-16 | 🟡坑 | 夜盘 trade_date 归属：验证误用日历日期 |
| M027 | 2026-08-17 | 🟡坑 | digest 的 regime / MAE-MFE 分析对小周期大区间极慢（3 年 5m 占回测 98%） |
| M028 | 2026-08-18 | ✅已修复 | "单边口径"实现把 max 写成加法，退化成毛持仓恒等式 |
| M029 | 2026-08-18 | 🟡坑 | 2H/4H 聚合口径：引擎与天勤一致（自然时钟），与同花顺/东财不一致（交易日累计） |
| M030 | 2026-08-19 | 🟡坑 | 前端改动时绝对路径与 rg 正则转义误用导致补丁和搜索失败 |
| M031 | 2026-08-19 | 🟡坑 | 金字塔伪单位代码与行情标的混层，导致同一主连被按多个代码重复展示 |
| M032 | 2026-08-19 | 🔴错误 | metrics 最大组合/单票仓位只取多头峰值，与持仓与风险 Tab 不一致 |
| M033 | 2026-08-20 | 🟡坑 | 交易 Tab 全量加载未贯通分类/标的/tab 状态，逻辑组伪单位仍被拆成多个标的 |
| M034 | 2026-08-20 | 🟡坑 | 交易 Tab 默认预览仍随首个响应传输全量交易，大数据量全表渲染可能卡顿 |
| M035 | 2026-08-20 | 🟡坑 | entry_mode/exit_mode 的三种常见非对称成交时序当前不支持 |
| M036 | 2026-08-20 | 🟡坑 | 多交易日历下目标权重缩放不等于实际组合持仓上限 |
| M037 | 2026-08-23 | 🟡坑 | Windows 全量 pytest 混入 symlink/可选依赖/共享状态基线失败，需按改动面归因 |

## 正文

### M001 · 腾讯长区间静默截断 500 根（2026-08-10）· 🟡坑
- 现象：腾讯接口单次最多返回 500 根且不报错，长区间被静默截断。
- 教训：分页边界要显式验证；数据源修复后升缓存版本让旧坏缓存失效。
- 状态：已修复（2026-08-10，V003 分页 + 缓存版本 3→4）
- 关联：解法见 HowToUse 8.35；来源 V003

### M002 · cmd 下 Rich 输出 ANSI 乱码（2026-08-07）· 🟡坑
- 现象：cmd 显示 `[36m` 等转义码；注册表 VirtualTerminalLevel 实测不生效。
- 教训：旧控制台不解析 ANSI，Win32 控制台 API 从根源解决（`legacy_windows=True`）。
- 状态：已修复（2026-08-07，theme.py）
- 关联：解法见 HowToUse 8.6；来源 V001

### M003 · 日志乱码"驱动器 E 中的卷没有标签"（2026-08-07）· 🟡坑
- 现象：cmd `dir` 输出被按 UTF-8 误解码成乱码。
- 教训：Windows 系统编码与 UTF-8 并存，解码先 UTF-8 后系统编码。
- 状态：已修复（2026-08-07，bash_tool.py）
- 关联：解法见 HowToUse 8.11

### M004 · run_dir outside allowed run roots（2026-08-07）· 🟡坑
- 现象：切源码版后回测子进程临时 HOME，run root 被解析到 temp 被拦截。
- 教训：沙箱 HOME 重定向改变路径解析，需显式白名单（VIBE_TRADING_ALLOWED_RUN_ROOTS）。
- 状态：已修复（2026-08-07，.env 追加白名单）
- 关联：解法见 HowToUse 8.13

### M005 · 东财 suggest 接口返回 JSONP 兜底页（2026-08-07）· 🟡坑
- 现象：searchapi.eastmoney.com 对 Python HTTPS 返回 JSONP 兜底页，`response.json()` 报 Expecting value。
- 教训：边缘节点会改写响应格式；先取原文再剥 JSONP，HTTP 比 HTTPS 稳。
- 状态：已修复（2026-08-07）
- 关联：解法见 HowToUse 8.15

### M006 · 改 Python 源码后 WebUI 不生效（2026-08-07）· 🟡坑
- 现象：serve/dev 后端未开 reload，改代码只刷新页面无效。
- 教训：FastAPI 未开 reload 时新代码要重启进程；CLI 每次是新进程直接生效。
- 状态：有效（行为性，非 bug）
- 关联：解法见 HowToUse 8.17

### M007 · SSE 流式中断 WinError 10054（2026-08-07）· 🟡坑
- 现象：agent 流式读取长流中途被强关；同环境非流式/curl 正常。
- 教训：流式长连接不稳定是服务端/中转问题，双失败降级非流式兜底。
- 状态：已修复（2026-08-07，loop.py 非流式降级）
- 关联：解法见 HowToUse 8.18

### M008 · 策略任务烧 token + 前端"等待后已停止"（2026-08-07）· 🔴错误
- 现象：AST 门禁反复打回→改代码→重跑循环，一次任务 15+ 轮烧 90 万 token；SSE 超时前端显示已停止但后端仍在跑。
- 教训：生成代码前对照 AST 安全约束自检；调大 VIBE_TRADING_SSE_TIMEOUT。
- 状态：已预防（2026-08-07，SKILL.md 约束 + SSE 超时配置）
- 关联：解法见 HowToUse 8.19/8.20

### M009 · data-bridge config.yaml 被存成二进制（2026-08-08）· 🟡坑
- 现象：Office 另存为生成 OLE 二进制（文件头 `D0 CF 11 E0`），VSCode 提示二进制/记事本乱码。
- 教训：config 必须纯文本 UTF-8 无 BOM。
- 状态：已规避（坏文件备份为 .bad）
- 关联：解法见 HowToUse 8.21

### M010 · source=local 报 missing local: 前缀（2026-08-08）· 🟡坑
- 现象：local loader 返回的 key 剥掉 `local:` 前缀，runner 按原样比对误判缺失并回退网络源。
- 教训：前缀剥离要两端一致；local 源失败即停止不回退网络。
- 状态：已修复（2026-08-08）
- 关联：解法见 HowToUse 8.25

### M011 · identity_mismatch / 沪深300 基准取不到（2026-08-12）· 🟡坑
- 现象：会话锁定单标的，换标的被拦（identity_mismatch）；免费链路取 000300 不稳定返回 0 候选。
- 教训：真实基准要 data-bridge 配 000300.SH + config 显式 benchmark；无 benchmark 时 benchmark_return 是策略自身持有收益，不可当真。
- 状态：有效（行为性约束）
- 关联：解法见 HowToUse 8.26/8.34

### M012 · 显示 SUCCESS 但无报告（2026-08-08）· 🔴错误
- 现象：agent 迭代耗尽只生成 config/代码没调回测，状态仍 success；`--show` 报 TypeError。
- 教训：判定成功看 run_card + metrics 是否真存在；`--check` 一键核查。
- 状态：已修复（2026-08-08）
- 关联：解法见 HowToUse 8.31

### M013 · loader 缓存不过期/坏数据反复命中（2026-08-10）· 🟡坑
- 现象：缓存无自动过期；腾讯 500 根截断 bug 的数据被缓存反复命中。
- 教训：数据源修复后要升缓存版本；可疑结果先清缓存再重跑。
- 状态：已修复（2026-08-10，缓存版本 3→4）
- 关联：解法见 HowToUse 8.35

### M014 · trades.csv 被 WPS/Excel 占用 PermissionError（2026-08-12）· 🟡坑
- 现象：重跑 runner 覆盖 trades.csv 时文件被占用报错。
- 教训：Windows 文件占用；跑前关掉打开该文件的编辑器。
- 状态：有效（环境性）
- 关联：解法见 HowToUse 8.37

### M015 · 前/后复权口径混淆（2026-08-12）· 🟡坑
- 现象：在线 loader 固定返回 qfq；用户表写"后复权"时拿 qfq 对不上（002133 区间 qfq -14.08% vs hfq -9.53%）。
- 教训：复权口径要显式核对；local 文件是什么口径就回测什么口径。
- 状态：有效（认知性）
- 关联：解法见 HowToUse 8.40

### M016 · backtest_end 纯日期截掉最后一天（2026-08-14）· 🟡坑
- 现象：backtest_end 纯日期被解析为 00:00，回测窗口最后一天丢失。
- 教训：纯日期边界要按整天包含处理。
- 状态：已修复（2026-08-14，V018）
- 关联：来源 V018

### M017 · K 线 tooltip OHLC 错位（2026-08-14）· 🟡坑
- 现象：O 随 K 线递增——tooltip 把 ECharts value 首位索引当 open。
- 教训：candlestick `params.value` 可能带前导维度；`params.data` 优先并做 OHLC 自检。
- 状态：已修复（2026-08-14，V019）
- 关联：来源 V019

### M018 · 交易表硬编码列导致新列不渲染（2026-08-15）· 🔴错误
- 现象：trades.csv 有 commission 但 WebUI 交易表不显示；清单里的浏览器验证被静默跳过。
- 教训：硬编码列表格组件新增字段必须主动加列+补组件测试；"数据链路正确"≠"每个 UI 展示面正确"。
- 状态：已修复（2026-08-15，TradesTab 加列）
- 关联：同全局 E003；来源 V024

### M019 · 策略文件被外部进程置零损坏（2026-08-15）· 🟡坑
- 现象：signal_engine.py 全 0 字节，daily 备份亦置零，一度无法重跑复验。
- 教训：重要策略文件要保留独立副本。
- 状态：已恢复（用户找回文件后重跑验证）
- 关联：来源 V024

### M020 · 触轨/止盈判定口径与预期相反（2026-08-16）· 🔴错误
- 现象：代码"触轨用收盘穿越 band、止盈用盘中触及 mid"，用户预期"盘中触轨 + 收盘穿越 mid"，出场时机普遍错位。
- 教训：判定口径（收盘 vs 盘中）是策略语义最高发根因，实现前必须书面 if-then 清单逐条确认（策略三步协议）。
- 状态：有效（拍板点 A-F 待定）
- 关联：来源策略三步协议（语义清单确认→实现→重放复述验收）

### M021 · 郑商所代码后缀 .CZCE 匹配不上被误判 A 股引擎（2026-08-16）· 🟡坑
- 现象：本地回测 FG/RM 用 `FG0000.CZCE`，数据正常加载、回测跑完，但 0 笔交易、无任何报错/警告（metrics 里 `rebalance_count>0` 而 `trade_count=0`）。
- 教训：引擎市场识别正则只认 `ZCE|DCE|SHFE|INE|CFFEX|GFEX`，郑商所后缀必须写 `.ZCE`；"配置加载成功"≠"引擎路由正确"，0 成交且无警告时先查代码后缀是否被误判成 A 股引擎。
- 状态：有效（约定）
- 关联：解法见 HowToUse 8.22

### M022 · 直跑 runner 不加载 <vibe_home>/.env，缓存开关实际失效（2026-08-16）· ✅已修复
- 现象：`VIBE_TRADING_DATA_CACHE=1` 写在 `.vibe-trading/.env`，但 `python -m backtest.runner` 每次仍重新取数，cache 目录不产生新 parquet；`loader_cache_enabled()` 实测 False。
- 教训：runner 的 `load_dotenv()` 无参只找 agent/.env 与 cwd/.env（仓库里都没有），`.vibe-trading/.env` 从不被加载；CLI/UI 走 `_ensure_dotenv()` 才正确。直跑 runner 需显式 `VIBE_TRADING_DATA_CACHE=1` 前缀；缓存 key 按 源+标的+周期+区间 区分，换区间是新 key（非失效）。
- 状态：已修复（2026-08-22，计划 P-20260816-cache_env_once）
- 已修复：2026-08-22 · runner 复用统一 `_ensure_dotenv()`；默认读取 `~/.vibe-trading/.env`，`VIBE_TRADING_HOME` 启动前覆盖时读取自定义运行目录 `.env`；隔离子进程回归验证缓存首次写入、二次命中。
- 关联：解法见 HowToUse 8.35；计划 P-20260816-cache_env_once；来源 V042
- 补充（2026-08-22）：Windows 测试中仅 monkeypatch `HOME` 不会改变 `pathlib.Path.home()`；验证自定义运行目录应设置 `VIBE_TRADING_HOME`，避免将既有路径基线失败误判为 runner 缓存回归。

### M023 · 保本止损"同 bar 回溯"假象：bar 级把整根高低点当同时发生（2026-08-16）· 🟡坑
- 现象：加"浮盈≥1R 止损上移成本价"规则，同 bar 生效口径下结果 -1,224；逐笔排查发现 3 笔在触发 bar 内被"新止损"平掉——真实时间线里触发发生在最低点之后、价格从未回到成本价，出场是虚构的（修正为下一根生效后 -10）。
- 教训：bar 级逻辑无法还原 bar 内先后顺序，"盘中触发+盘中执行"类规则要先定口径（同 bar 生效 vs 下一根生效）；保守口径（先按当前止损判出场、再更新止损）避免虚构交易。
- 状态：有效（方法论认知）
- 关联：无

### M024 · 主连切换日跳空污染切换后约 20 根指标（2026-08-16）· 🟡坑
- 现象：主连连续序列在切换日价格跳空，切换后 MA/BOLL/MACD/强度等滚动指标由新旧合约混价算出，切换日附近指标失真。
- 教训：用主连连续数据回测，入场信号距最近切换日的距离要检查；本会话实测过滤+禁开日使入场无一落在污染窗口（约 20 根），但每个新策略需单独验证。
- 状态：有效（认知性）
- 关联：计划 P-20260816-contract_switch_auto（切换日自动识别）

### M025 · trades.csv 归因陷阱：reason 列失真 + qty 随权益漂移（2026-08-16）· 🟡坑
- 现象：sub agent 按 reason 列归因把 V3.51 的 -6,750 差说成"+10,220 大单被挤掉"，被用户逐笔质疑后推翻；实际是同一笔单入场提前、手数 7→6（-1,400）+ 新增单净亏（-4,890）+ 佣金（-1,188），闭合校验通过。
- 教训：① reason 列被引擎硬编码为 "signal"，不能用来判平仓类型；② 手数 = f(权重×权益×杠杆/价)，权益路径一变后面所有单 qty 微调，逐笔配对要按 方向+入场时间 而非 qty；③ 收益差异必须逐笔闭合（Δfinal ≈ ΔΣpnl − ΔΣ佣金），否则归因不可信。
- 状态：有效（方法论认知）
- 关联：无

### M026 · 夜盘 bar 归下一交易日：验证切换日规则误用日历日期（2026-08-16）· 🟡坑
- 现象：RM v35 验证"禁开日开仓"报 1 笔违规（2026-04-14 21:30 夜盘入场），实为验证脚本用日历日期（timestamp.date）而非 trade_date——该 bar 的 trade_date 是 2026-04-15（正常交易日），策略未违规。
- 教训：期货夜盘 bar 按 trade_date 归下一个交易日；凡涉及"交易日归属"的验证/规则（切换日、禁开日、换约）必须用 trade_date 字段，用日历日期会误报。
- 状态：有效（认知性）
- 关联：无

### M027 · 回测后 digest 的 regime / MAE-MFE 分析对小周期大区间极慢（2026-08-17）· 🟡坑
- 现象：3 年 5m 回测缓存命中后总耗时 766s，其中回测后 digest（分析数据生成）约 750s 占 98%（引擎执行仅 ~14s）；1 个月 5m 全程只要 16s。digest 慢在：①相关性 regime（`compute_edge_density`：每个 bar 做一次 60-bar 窗口的 pandas 滚动相关性，O(bar×window)，5m 三年 52,626 bar 要算 5 万多次 corr）；②MAE/MFE（`add_mae_mfe`：每笔交易遍历该标的全量 bar 筛持仓窗口，O(trades×bars)，1753×52,626≈9 千万次 Python 循环）。
- 教训：digest 的分析组件对 bar 数/交易数敏感，成本可远超回测本身（3 年 5m 从 15m 的几秒膨胀到 750s）；WebUI 分析图依赖 digest，但 regime（相关性状态）对单标的/伪单位组合价值有限、MAE-MFE 属诊断性指标，大区间小周期调参时可整体跳过。
- 状态：有效（缓解：P-20260817-fastrun 已实现 --fastrun / --without-regime / --without-mae-mfe 跳过这两项，3 年 5m 回测 766s→约 32s）
- 关联：计划 P-20260817-fastrun；同全局 E010

### M028 · "单边口径"实现时把 max 写成加法，退化成毛持仓恒等式（2026-08-18）· ✅已修复
- 现象：实现账户风险度的"单边（按 weight_groups 分组取多空大边）"时写成 `pos_sum + |neg_sum|`，单测发现单边恒等于毛持仓——因为 `sum(正部) + sum(|负部|) == sum|w|`（毛持仓）是**数学恒等式**，不是取大边。锁仓样本（同组多空各 20%）应得 20%，实现却得 40%。正确做法是 `max(组内多头和, |组内空头和|)` 再跨组合计。
- 教训：毛 / 净 / 单边三种口径容易互相踩恒等式——毛 = 正部和 + |负部和|、净 = 正部和 − 负部和、单边 = max(正部和, |负部和|)。写实现前先用**锁仓小样本**（同组多空各 20%）验证三个口径是否分别得到 40% / 0% / 20%，再上真实数据；单测里必须包含锁仓场景断言。
- 状态：已修复（V034；测试 `test_daily_position_and_risk_aggregation` 断言锁仓日 gross=40 / net=0 / single=20，防回归）
- 关联：计划 P-20260818-daily_position_risk_charts

### M029 · 2H/4H 聚合口径：引擎与天勤一致、与行情软件不一致（2026-08-18）· 🟡坑
- 现象：vibe-trading、天勤与同花顺/东方财富的 2H/4H 聚合时间口径不同，TA（郑商所棉花）实测对比如下：

  | 数据源 | 对齐方式 | TA 2H（桶标签 → 实际覆盖交易时段） | TA 4H | 与 vibe-trading 的关系 |
  |---|---|---|---|---|
  | vibe-trading local loader | **自然时钟，零点对齐**（pandas resample） | **6 根/天**：08:00→09:00-09:59；10:00→10:00-11:30；12:00→13:30-13:59；14:00→14:00-15:00；20:00→21:00-21:59；22:00→22:00-23:00（自然桶分别为 08-10、10-12、12-14、14-16、20-22、22-24） | 08:00 / 12:00 / 20:00 三根/天；分别覆盖上午 09:00-11:30、下午 13:30-15:00、夜盘 21:00-23:00 | 基准口径；引擎当前不支持 2H |
  | 天勤 TqSdk（快期服务端，KQ.m@CZCE.TA） | **自然时钟，零点对齐** | 支持 7200 秒周期，口径与左列一致 | bar 起点与 vibe-trading 一致；已验证 08:00 bar 的 OHLCV = 1H 的 09:00+10:00+11:00 三根聚合 | **一致**；日线另按交易日锚定，夜盘归次日 |
  | 同花顺 / 东方财富等行情软件 | **交易日锚定 + 交易时间累计切分** | 3 根/天：21:00-23:00 / 09:00-11:15 / 11:15-15:00 | 2 根/天：21:00→次日 11:15 满 4 小时 + 尾段 | **不一致**；同花顺 4H 与引擎/天勤是“两根对三根” |
- 教训：①判断某数据源的周期聚合口径**必须拉真实数据实测**，不能凭字段/文档推断（曾从 trading_time 字段 + 日线锚定推断天勤是同花顺式，实测推翻）；②vibe-trading 本地数据与天勤同口径（自然时钟），与同花顺/东财不同口径——拿行情软件 K 线图去对引擎/天勤的策略信号会错位（同花顺 4H 两对三），看图以策略实际数据源为准；③2H 是自然时钟碎桶（每根半截时段），策略语义与 1H/4H 不同；vibe-trading 引擎不支持 2H，天勤支持（7200 秒）。
- 状态：有效（认知性）
- 关联：HowToUse 8.42（引擎支持周期清单，均为自然时间聚合）

### M030 · 前端改动时绝对路径与 rg 正则转义误用（2026-08-19）· 🟡坑
- 现象：第一次 `apply_patch` 把组件写成了错误的工作区绝对路径；随后一次 `rg` 命令的转义使正则不闭合；两次均在业务改动前失败。
- 教训：仓库内文件修改统一以实际仓库绝对路径为准；PowerShell 下 `rg` 优先使用简单单引号模式，先验证搜索命令再继续。
- 状态：已规避（2026-08-19，改用 `E:\gitCloneProgram\vibe-trading-src` 路径和单引号 `rg` 模式，后续测试/构建通过）
- 关联：计划 P-20260819-chart_4h_interval

### M031 · 金字塔伪单位代码与行情标的混层（2026-08-19）· 🟡坑
- 现象：为支持单标的四单位金字塔，把同一份 TA 主连 CSV 注册为 `TA0001.ZCE`~`TA0004.ZCE`；runner 为每个代码写出相同的 `ohlcv_*.csv`，WebUI 又按文件代码生成四个行情标的。
- 根因：当前策略用 `codes[idx]` 把代码当作单位槽位，回测引擎与 UI 也把执行代码直接当作行情标的，没有独立的 logical symbol / execution leg 元数据层。
- 教训：同一行情源的执行腿/伪单位不能复用真实标的代码语义；否则行情、交易归因、持仓分组和图表都会把一个标的拆成多个标的。
- 状态：有效（设计性问题；尚未重构）
- 已缓解：2026-08-19 · V038 将逻辑分组迁移到 config，指标/digest/WebUI 不再把伪单位当作独立逻辑标的；执行层重复 OHLCV 快照仍待 B 方案消除。
- 关联：本次 TA 交接诊断；解法需单独建立中/大改动计划。

### M032 · metrics 最大组合/单票仓位只取多头峰值，与持仓与风险 Tab 不一致（2026-08-19）· 🔴错误
- 现象：v437fix9 的 `metrics.csv` / 报告顶部显示 `max_portfolio_weight=max_single_weight=10.91%`；同一 run 的 `positions.csv` 经 WebUI「持仓与风险」Tab 按毛/单边口径计算，最大值为 18.75%。数据本身一致：2024-07-23 的四个 TA 伪单位合计空头 -18.75%，2026-03-04 的多头合计为 +10.91%。
- 根因：`agent/backtest/engines/base.py` 用带符号权重求和后直接 `.max()`，以及逻辑分组后直接取 `.max()`，负向空头峰值被排除；而 `agent/backtest/analysis/digest.py` 的 Tab 使用 `gross=sum|w|`、`net=sum(w)`、`single=max(多头和,|空头和|)`，两者的最大值语义不同。
- 教训：所有名为“最大”的仓位指标必须明确是否包含空头及采用净、毛还是单边口径；标量指标和时序图必须复用同一套聚合定义，不能对带符号序列直接调用 `.max()` 代替多空完整峰值。
- 状态：有效（未修复；计划讨论中）
- 关联：计划 P-20260818-position_weight_magnitude

### M033 · 交易 Tab 全量加载与逻辑标的筛选未贯通（2026-08-20）· 🟡坑
- 现象：后端响应已经同时提供 `trade_log` 预览和 `artifacts_trades_csv` 全量交易，但交易 Tab 仍按预览源工作；分类按钮和表格有 100 行显示闸门，点击全量后的状态尚未覆盖分类、交易标的、图表标的和 tab 切换。交易标的筛选直接按交易行 raw `code` 去重，TA0001/TA0002/TA0003 等同一主连伪单位会被显示成多个标的。
- 根因：全量选择状态若放在会随 tab 卸载的 `TradesTab` 内会丢失；交易统计/分类/表格没有统一绑定活动数据源；V038 已提供 `chart_groups` 逻辑分组元数据，但交易 Tab 尚未复用该来源。
- 教训：全量数据、筛选统计、分页展示和逻辑标的归因必须共享同一套活动数据源与 group resolver；不能因为表格首屏分页或执行 code 不同而把预览/伪单位误当成完整交易集/不同标的。
- 状态：已修复（2026-08-20 · V039；代码已验证，待提交）
- 关联：计划 P-20260817-trade_log_full_load；逻辑分组来源 P-20260819-logical_symbol_groups

### M034 · 交易 Tab 默认预览与全量传输/渲染边界混淆（2026-08-20）· 🟡坑
- 现象：后端在生成 `trade_log[:500]` 预览的同时，已经把完整 `artifacts_trades_csv` 放进首次 `/runs/{id}` 响应；5000~10000 笔交易时首次 payload 约 1~3MB，点击全量后若直接创建全部表格 DOM，可能进入秒级等待和滚动卡顿。
- 根因：预览行数、网络传输、前端内存数据和 DOM 可见行数是四个不同层次，当前设计只限制了预览/分页，没有把全量数据访问与窗口化渲染分开。
- 教训：大表格要分别测传输、解析、统计和 DOM 渲染；“默认只显示 500”不等于“只加载 500”，全量数据可用也不等于全量 DOM 常驻。
- 状态：部分缓解（2026-08-20 · V039 已用窗口化表格避免全量 DOM；首次响应传输全量交易仍是既定设计边界）
- 关联：计划 P-20260817-trade_log_full_load；需采用窗口化/虚拟列表，若要真正按需传输另建后端计划。

### M035 · 三种混合成交时序组合未支持（2026-08-20）· 🟡坑
- 现象：当前引擎拒绝三种合理的非对称成交组合：`entry_mode=next_open` + `exit_mode=close`、`entry_mode=next_open` + `exit_mode=stop`、`entry_mode=close` + `exit_mode=next_open`；现有引擎只允许 `next_open/next_open`、`close/close`、`close/stop`。
- 根因：`BaseEngine` 用开仓模式同时决定信号是否整体 shift、`_same_bar` 状态和成交价格路径，开仓与平仓没有独立的时序/成交处理；同一信号下先收盘平旧仓、下一根开盘开新仓，以及开仓后持续维护止损状态，都没有统一建模。
- 教训：开仓时点、平仓时点和止损激活时点应分别建模；常见的非对称成交时序不能用固定组合白名单代替。
- 状态：部分修复（2026-08-23 · V043）——旧的二元字段语义已迁移为 `entry_mode` / `exit_mode` / `stop_loss_mode` 三字段；`next_open/stop` 已支持，`next_open/close` 与 `close/next_open` 仍按本期范围明确拒绝。旧 `exit_mode=stop` 不再静默解释，runner 会 warning 并失败。
- 关联：解法说明见计划 `documents/plans/P-20260822-risk_exit_execution_modes.md` 与 HowToUse 8.34；实现入口 `agent/backtest/engines/base.py`；边界测试 `agent/tests/test_engine_execution_modes.py` / `agent/tests/test_runner_coverage.py`。

### M036 · 多交易日历下目标权重缩放不等于实际组合持仓上限（2026-08-20）· 🟡坑
- 现象：六品种组合先把目标权重帧压到组合 60%、单标的 50%，但 `positions.csv` 仍出现组合峰值 62.48%。原因是某品种在另一品种新增仓位的时间点没有对应 bar，最终引擎按各 code 自己的交易日历前向填充；已有同方向 Position 也不会因目标权重变小而自动缩仓。
- 根因：目标权重约束、统一时间轴对齐和实际持仓生命周期是三个不同层次；`constraints` 只作用于 optimizer 输出，且引擎对已有同方向仓位不执行目标权重缩放。
- 解决：组合实验在新增伪单位开仓/加仓前检查当前已有目标仓位与新增单位的毛敞口，超出组合/逻辑标的上限就拒绝新增；完成回测后必须从 `positions.csv` 按 `logical_groups` 重算峰值，不能只看策略内的目标权重或单一指标字段。修正后的组合 run 实测组合毛峰值 59.90%、单标的峰值 47.46%。
- 教训：多标的仓位上限必须在“新增持仓决策”和“最终持仓工件”两个层面验收；不同交易日历下，事后统一缩放目标权重可能无法约束已经存在的仓位。
- 状态：已规避（2026-08-20 · 组合策略快照采用新增仓位前门控，并用 `positions.csv` 复核；引擎通用的同方向仓位缩放能力仍未补齐）
- 关联：`config.logical_groups` 分组口径见 HowToUse 8.43；组合 run 的实现留在对应 run 的 `code/signal_engine.py`。

### M037 · Windows 全量 pytest 的环境基线失败与改动面归因（2026-08-23）· 🟡坑
- 现象：本次全量 pytest 共 `9526 passed, 86 skipped`，另有 `85 failed, 9 errors`；失败集中在 Windows 无 symlink 权限、可选/网络数据源模拟、跨测试共享状态等，与本次 MCP/runner/analysis 改动面无直接重合。
- 教训：全量套件退出码不能单独证明本次改动失败，也不能被忽略为“全绿”；应保留全量结果并按改动面重跑安全、MCP、runner、digest、analysis、文档和真实小 run 回归。
- 状态：有效（环境性；本次改动面定向回归 204 passed、1 skipped，前端 458 passed，build 通过）
- 关联：来源迭代 V044；通用分层核对方法见全局日志 E114。
